[package]
name = "fpm"
version = "0.1.0"
edition = "2024"
license = "GPL-3.0"

[dependencies]
toml = "1.1"
serde = { version = "1.0", features = ["derive"] }
clap = { version = "4.6.1", features = ["derive"] }
